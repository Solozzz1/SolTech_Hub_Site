# Contact Us

Get in touch with us to discuss your project requirements or schedule a consultation. We offer competitive pricing in USD, making our services accessible to businesses of all sizes.

## Contact Information

- **Email**: solomonmukonda0@gmail.com
- **Phone**: +263782980290 / +263719110713
- **Address**: Tech Innovation Park, Harare Zimbabwe

## Send Us a Message

<div className="contact-form">
  <form id="contactForm" onSubmit={(e) => {
    e.preventDefault();
    const form = e.target;
    const submitButton = form.querySelector('button[type="submit"]');
    
    submitButton.disabled = true;
    submitButton.textContent = 'Sending...';
    
    const formData = new FormData(form);
    const data = Object.fromEntries(formData.entries());
    
    fetch('/api/contact', {
      method: 'POST',
      headers: {
        'Content-Type': 'application/json',
      },
      body: JSON.stringify(data),
    })
    .then(response => {
      if (!response.ok) {
        return response.json().then(err => Promise.reject(err));
      }
      return response.json();
    })
    .then(() => {
      alert('Thank you for your message! We will get back to you soon.');
      form.reset();
    })
    .catch(error => {
      alert('Sorry, there was an error sending your message: ' + (error.message || 'Failed to send message'));
    })
    .finally(() => {
      submitButton.disabled = false;
      submitButton.textContent = 'Send Message';
    });
  }}>
    <div className="form-group">
      <label htmlFor="name">Name</label>
      <input type="text" placeholder="Name" id="name" name="name" required />
    </div>

    <div className="form-group">
      <label htmlFor="email">Email</label>
      <input type="email" placeholder="Email" id="email" name="email" required />
    </div>

    <div className="form-group">
      <label htmlFor="company">Company</label>
      <input type="text" placeholder="Company" id="company" name="company" />
    </div>

    <div className="form-group">
      <label htmlFor="project">Project Description</label>
      <textarea id="project" placeholder="Project Description" name="project" rows={4} required></textarea>
    </div>

    <div className="form-group">
      <label htmlFor="budget">Budget Range (USD)</label>
      <select id="budget" name="budget" required>
        <option value="">Select a range</option>
        <option value="small">$1,000 - $5,000 USD</option>
        <option value="medium">$5,000 - $20,000 USD</option>
        <option value="large">$20,000+ USD</option>
      </select>
      <small className="budget-note">* All prices are in USD. We offer flexible payment terms and competitive rates for Zimbabwean businesses.</small>
    </div>

    <div className="form-group">
      <label htmlFor="timeline">Timeline</label>
      <select id="timeline" name="timeline" required>
        <option value="">Select a timeline</option>
        <option value="urgent">Urgent (1-2 months)</option>
        <option value="standard">Standard (3-6 months)</option>
        <option value="flexible">Flexible (6+ months)</option>
      </select>
    </div>

    <button type="submit" className="submit-button">Send Message</button>
  </form>
</div>

<style jsx>{`
  .contact-form {
    max-width: 600px;
    margin: 2rem auto;
    padding: 2rem;
    background: white;
    border-radius: 0.5rem;
    box-shadow: 0 4px 6px rgba(20, 111, 202, 0.1);
    border: 1px solid rgba(20, 111, 202, 0.1);
  }
  .form-group {
    margin-bottom: 1.5rem;
  }
  label {
    display: block;
    margin-bottom: 0.5rem;
    font-weight: 500;
    color: var(--brand-blue);
  }
  input, select, textarea {
    width: 100%;
    padding: 0.75rem;
    border: 1px solid rgba(20, 111, 202, 0.2);
    border-radius: 0.25rem;
    font-size: 1rem;
    transition: all 0.3s ease;
  }
  input:focus, select:focus, textarea:focus {
    outline: none;
    border-color: var(--brand-orange);
    box-shadow: 0 0 0 3px rgba(255, 144, 69, 0.1);
  }
  textarea {
    resize: vertical;
  }
  .submit-button {
    background: var(--brand-blue);
    color: white;
    padding: 0.75rem 1.5rem;
    border: none;
    border-radius: 0.25rem;
    font-size: 1rem;
    cursor: pointer;
    width: 100%;
    transition: all 0.3s ease;
  }
  .submit-button:hover {
    background: var(--brand-orange);
    transform: translateY(-2px);
    box-shadow: 0 4px 6px rgba(255, 144, 69, 0.2);
  }
  .submit-button:disabled {
    background: #ccc;
    cursor: not-allowed;
    transform: none;
    box-shadow: none;
  }
  .budget-note {
    display: block;
    margin-top: 0.5rem;
    color: var(--brand-blue);
    font-size: 0.875rem;
  }
`}</style> 